info:
  name: "list all "
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:51620/new_vehicles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
